@model IEmployeeService

<form asp-controller="employees" asp-action="Invade"><input type="submit" value="Invade" /></form>
<form asp-controller="employees" asp-action="Genocide"><input type="submit" value="Genocide" /></form>

    <p>@Model.KillCount</p>
    <ul>
        @foreach (var employee in Model.GetAll())
        {
            <li><a asp-controller="Employees" asp-action="Details" asp-route-id="@employee.Id">@employee.Name</a></li>
        }


    </ul>

